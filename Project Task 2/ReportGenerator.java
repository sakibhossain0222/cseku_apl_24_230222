package RailwayManagement;

// ReportGenerator.java (Open/Closed Principle - OCP)
// Interface for generating reports, allowing different types of reports to be generated.

public interface ReportGenerator {
    // Method to generate a report.
    void generateReport();
}
